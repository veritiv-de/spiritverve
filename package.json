{
  "name": "helloworld-amplify-app",
  "version": "1.0.0",
  "description": "A minimal full-stack HelloWorld app using React and Node.js, hosted via AWS Amplify",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "mock-server": "node mock-server.js",
    "dev": "concurrently \"npm run mock-server\" \"npm start\""
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "pg": "^8.16.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:3001",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
